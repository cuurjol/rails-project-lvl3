<% %w[music books].each do |category| %>
  <% Bulletin.aasm.states.map(&:name).each do |state| %>
    <%= category %>_<%= state %>_thumbnail_blob: <%= ActiveStorage::Blob.fixture('filename'.to_sym => "#{category}.jpg") %>
  <% end %>
<% end %>
