= "#{t('mail_form.bulletins.contact.main_text_html', website: root_url)}\r\n\r\n"

= "#{t('mail_form.bulletins.contact.info_letter_text')}\r\n"

== "1. #{@resource.class.human_attribute_name(:name)}: #{@resource.name}\r\n"
= "2. #{@resource.class.human_attribute_name(:email)}: #{@resource.email}\r\n"
= "3. #{@resource.class.human_attribute_name(:bulletin_id)}: #{bulletin_url(@resource.bulletin)}\r\n\r\n"

= "#{@resource.class.human_attribute_name(:message)}:\r\n"
= "#{'-' * 100}\r\n"
== @resource.markdown_message
= "#{'-' * 100}\r\n"
